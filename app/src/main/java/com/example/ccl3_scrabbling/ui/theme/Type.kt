package com.example.ccl3_scrabbling.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.example.ccl3_scrabbling.R

val Roboto = FontFamily.Default
val Calibri = FontFamily(
    Font(R.font.calibri)
)
val Gothic = FontFamily(
    Font(R.font.old_london)
)
val Bubbly = FontFamily(
    Font(R.font.bubbly)
)
val Epic = FontFamily(
    Font(R.font.epic)
)
val Rune = FontFamily(
    Font(R.font.rune)
)
val Blocky = FontFamily(
    Font(R.font.minecraft)
)
val Harry = FontFamily(
    Font(R.font.harry)
)
val Ninja = FontFamily(
    Font(R.font.ninja)
)
val Psychedelic = FontFamily(
    Font(R.font.psychedelic)
)
val Vehicular = FontFamily(
    Font(R.font.vehicular)
)
val Times = FontFamily(
    Font(R.font.times_new_roman)
)
val Bold = FontFamily(
    Font(R.font.bold)
)
val Squid = FontFamily(
    Font(R.font.squid)
)
val Tropics = FontFamily(
    Font(R.font.tropics)
)
val Newspaper = FontFamily(
    Font(R.font.newspaper)
)
val NASA = FontFamily(
    Font(R.font.nasa)
)
val Lobster = FontFamily(
    Font(R.font.lobster)
)
val Graffiti = FontFamily(
    Font(R.font.graffiti)
)
val Dutch = FontFamily(
    Font(R.font.dutch)
)
val Design = FontFamily(
    Font(R.font.design)
)
val College = FontFamily(
    Font(R.font.college)
)
val Barney = FontFamily(
    Font(R.font.barney)
)
val Alhambra = FontFamily(
    Font(R.font.alhambra)
)


// Set of Material typography styles to start with
val Typography = Typography(
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        lineHeight = 24.sp,
        letterSpacing = 0.5.sp
    )
    /* Other default text styles to override
    titleLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 22.sp,
        lineHeight = 28.sp,
        letterSpacing = 0.sp
    ),
    labelSmall = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Medium,
        fontSize = 11.sp,
        lineHeight = 16.sp,
        letterSpacing = 0.5.sp
    )
    */
)